# 音视频播放网站开发需求文档（PRD）

> 部分内容由豆包生成
> 
> 

**文档状态**：初稿 v1\.0 ｜ **创建日期**：2026\-08\-31 ｜ **适用阶段**：需求评审与开发启动

**核心定位**：一款支持 m3u / m3u8 播放列表的在线音视频播放网站，提供流畅的流媒体播放体验。

# 项目概述

## 背景与问题

m3u / m3u8 是目前 IPTV、网络直播、HLS 流媒体领域最通用的播放列表格式，广泛应用于电视直播、影视点播、音频广播等场景。然而，普通用户缺乏一个轻量、易用、跨平台的网页端播放器来直接加载和播放这类播放列表，往往需要安装专用客户端或使用复杂的本地工具。

本项目旨在构建一个纯网页端的音视频播放网站，用户只需粘贴或上传 m3u / m3u8 播放列表地址，即可在浏览器中直接播放其中的音视频内容，无需安装任何插件或客户端。

## 产品目标

- 支持用户通过 URL 或本地文件加载 m3u / m3u8 播放列表

- 实现播放列表的解析、分类展示与频道/曲目切换

- 提供稳定流畅的 HLS 流媒体播放能力，兼容主流浏览器

- 首屏加载时间 ≤ 2 秒，视频起播时间 ≤ 3 秒（网络正常条件下）

- 播放成功率 ≥ 95%（排除源站不可用的情况）

## 目标用户

|用户类型|典型场景|核心诉求|
|---|---|---|
|普通观众|观看网络电视直播、影视点播|操作简单、播放流畅、频道好找|
|技术爱好者|测试自建流媒体源、调试 HLS 流|支持自定义源、播放信息透明、容错性强|
|内容运营者|快速预览和验证播放列表内容|批量加载、分类管理、播放状态可查|

# 范围定义

## 本期范围（In Scope）

- m3u / m3u8 播放列表的在线解析与加载（URL 导入 \+ 本地文件上传）

- HLS（\.m3u8 \+ \.ts 分片）音视频流的网页端播放

- 播放列表的分组（group\-title）解析与分类导航

- 播放器基础控制：播放/暂停、音量、进度条、全屏、倍速

- 最近播放记录与收藏功能（本地存储）

- 播放源的在线状态检测与不可用提示

- 响应式布局，适配桌面端与移动端浏览器

## 非目标（Out of Scope）

以下功能**不在本期开发范围内**，后续版本可评估纳入：

- 用户注册/登录系统与云端同步（本期仅使用浏览器本地存储）

- 视频转码、录制、下载功能

- DRM 加密内容的解密播放

- 社交分享、评论、弹幕功能

- 后台内容管理系统（CMS）

- RTMP、RTSP 等非 HLS 协议的直接播放（需转码网关，本期不做）

- 付费会员与支付系统

# 功能需求

## 播放列表加载

### URL 导入

**Actor**：用户 ｜ **触发**：用户在首页输入框粘贴 m3u/m3u8 链接并点击加载

**正常流程**：

1. 系统校验 URL 格式合法性（http/https 开头）

2. 通过代理或直接请求获取播放列表内容（处理跨域）

3. 解析播放列表，提取频道名称、Logo、分组、流地址

4. 展示加载成功提示，进入播放列表页面

**异常处理**：

- URL 格式错误 → 输入框下方红色提示"请输入有效的 http/https 链接"

- 请求超时（\>10s）→ 提示"加载超时，请检查链接是否可访问"

- 内容非 m3u/m3u8 格式 → 提示"无法识别的播放列表格式"

- 跨域被拒 → 自动切换至代理模式重试，仍失败则提示"该源存在跨域限制，建议使用支持 CORS 的源"

**验收标准**：合法的 m3u/m3u8 URL 能在 5 秒内完成解析并展示频道列表；异常场景均有明确用户可读的错误提示。

### 本地文件上传

**Actor**：用户 ｜ **触发**：用户选择本地 \.m3u / \.m3u8 / \.txt 文件上传

**正常流程**：

1. 用户点击"上传文件"按钮，选择本地播放列表文件

2. 系统读取文件内容（限制单文件 ≤ 5MB）

3. 解析并展示频道列表，文件名作为播放列表名称

**验收标准**：支持 \.m3u、\.m3u8、\.txt 后缀；文件内容符合 m3u 格式即可正确解析；超过 5MB 提示"文件过大，请精简后上传"。

## 播放列表解析与展示

### 解析规则

系统需兼容以下 m3u/m3u8 扩展标签：

|标签|必须性|说明|
|---|---|---|
|\#EXTM3U|必须|文件头标识，无此标签时尝试按纯 URL 列表解析|
|\#EXTINF|必须|频道/曲目信息，解析 duration、tvg\-id、tvg\-name、tvg\-logo、group\-title、name|
|\#EXTGRP|可选|分组名称，优先级低于 EXTINF 中的 group\-title|
|\#EXTVLCOPT|可选|VLC 选项，本期忽略|
|纯 URL 行|必须|实际流地址，支持 http/https|

### 分组导航

- 按 group\-title 字段对频道进行分组，无分组的归入"未分类"

- 左侧边栏展示分组列表，点击分组筛选右侧频道

- 支持分组搜索（按分组名称模糊匹配）

- 分组名称为空或重复时自动去重合并

### 频道列表

- 每个频道项展示：频道 Logo（有 tvg\-logo 时）、频道名称、在线状态指示

- 支持频道名称搜索（实时过滤，输入即搜）

- 点击频道项开始播放，当前播放项高亮显示

- 频道数量较多时（\>200）启用虚拟滚动，保证页面流畅

## 播放器核心功能

### 播放控制

|功能|行为描述|优先级|
|---|---|---|
|播放/暂停|点击播放按钮或视频区域切换播放状态，空格键快捷操作|P0|
|音量控制|音量滑块 0\-100，支持静音切换，记住上次音量设置|P0|
|进度条|点播内容显示播放进度，可拖动跳转；直播内容显示"LIVE"标识，不可拖动|P0|
|全屏|浏览器全屏 API，双击视频区域也可切换|P0|
|倍速播放|0\.5x / 0\.75x / 1x / 1\.25x / 1\.5x / 2x，仅对点播内容生效|P1|
|画中画|支持浏览器原生 Picture\-in\-Picture|P2|
|字幕|解析 HLS 内嵌字幕（WebVTT），支持开关与语言切换|P2|

### 播放状态与错误处理

- **加载中**：显示加载动画，超过 5 秒提示"正在缓冲，请稍候\.\.\."

- **播放中**：正常渲染视频画面，控制栏 3 秒无操作自动隐藏

- **缓冲中**：画面卡顿，显示缓冲指示器

- **播放失败**：显示错误信息（区分"源站不可用""格式不支持""网络中断"），提供"重试"和"切换下一个频道"按钮

- **自动重试**：播放失败后自动重试 1 次，仍失败则展示错误页

### 音频播放模式

当播放列表中的流为纯音频（无视频轨）时，播放器自动切换为音频模式：

- 展示频道 Logo 或默认音频封面作为视觉占位

- 保留播放控制栏（播放/暂停、进度、音量）

- 支持后台播放（页面切换标签时音频不中断）

## 播放记录与收藏

### 最近播放

- 自动记录最近播放的 20 个频道（含播放列表来源、频道名称、流地址、播放时间）

- 首页展示"最近播放"区域，点击可直接续播

- 数据存储于浏览器 localStorage，清除浏览器数据后丢失

### 收藏

- 用户可对频道标记收藏（心形图标）

- 收藏列表独立展示，支持搜索和移除

- 收藏上限 100 个，超出时提示"收藏已满，请清理后再添加"

## 播放列表管理

- 支持保存多个播放列表源（名称 \+ URL），上限 10 个

- 已保存的播放列表可一键切换加载

- 支持编辑名称、删除、刷新（重新拉取解析）

- 播放列表数据存储于 localStorage

# 非功能需求

## 性能要求

|指标|目标值|测量条件|
|---|---|---|
|首屏加载时间|≤ 2 秒|4G 网络，Chrome 最新版|
|播放列表解析时间|≤ 1 秒（1000 频道以内）|本地文件，主流配置设备|
|视频起播时间|≤ 3 秒|HLS 源正常，带宽 ≥ 5Mbps|
|播放卡顿率|≤ 2%|排除源站问题，统计播放时长中卡顿占比|
|内存占用|≤ 300MB|播放 1080p 视频 30 分钟后|

## 兼容性要求

|平台|支持版本|备注|
|---|---|---|
|桌面浏览器|Chrome 90\+、Edge 90\+、Firefox 88\+、Safari 14\+|Safari 原生支持 HLS|
|移动端浏览器|iOS Safari 14\+、Android Chrome 90\+|响应式适配|
|屏幕分辨率|≥ 375px（手机）至 ≥ 1920px（桌面）|响应式布局|

**Safari 特殊处理**：Safari 浏览器原生支持 HLS（video 标签直接播放 m3u8），其余浏览器需通过 MSE 播放。选用 video\.js 后由其内置 http\-streaming 模块自动处理这一差异，无需自研能力检测。

## 安全与合规

- **内容免责**：网站仅提供播放工具，不存储、不转码、不传播任何音视频内容；页面底部展示免责声明

- **XSS 防护**：播放列表中的频道名称、Logo URL 等字段需做 HTML 转义，防止注入

- **CORS 代理**：如需后端代理解决跨域，代理仅做请求转发，不缓存内容，设置合理的超时和大小限制

- **HTTPS**：网站必须部署在 HTTPS 下，混合内容（HTTP 源）需提示用户风险

- **隐私**：不收集用户播放列表和观看记录，所有数据仅存于用户本地浏览器

## 可用性要求

- 核心操作（加载列表、播放、切换频道）不超过 2 次点击

- 所有交互元素有明确的 hover / active / disabled 状态

- 错误提示使用用户可读语言，不暴露技术堆栈信息

- 支持键盘快捷键：空格（播放/暂停）、←/→（快退/快进 10s）、↑/↓（音量）、F（全屏）、M（静音）

# 技术架构建议

## 整体架构

本项目采用**前端为主 \+ 后端代理**的架构：

- **前端**：单页应用（SPA），负责播放列表解析、播放控制、UI 交互

- **后端**：提供 CORS 代理接口（本期必需），解决跨域播放列表加载问题；无数据库、无用户系统

- **存储**：浏览器 localStorage，存储播放列表源、收藏、最近播放

## 前端技术选型

|层面|推荐方案|选型理由|
|---|---|---|
|框架|Vue 3 \+ Vite|轻量、构建快、生态成熟，适合中型 SPA|
|UI 组件库|Element Plus 或 Naive UI|响应式组件丰富，减少重复造轮子|
|HLS 播放|video\.js 内置 http\-streaming（VHS）|业界标准的 HLS 播放能力，兼容 MSE 与 Safari 原生播放，容错性好|
|播放器封装|video\.js|业界最成熟的 HTML5 播放器，插件生态丰富，内置 http\-streaming 模块，自动兼容 Safari 原生 HLS 与其余浏览器 MSE 播放|
|状态管理|Pinia|Vue 3 官方推荐，轻量直观|
|路由|Vue Router|官方路由，支持历史记录|
|m3u 解析|自研解析器（正则 \+ 状态机）|m3u 格式简单，自研可精准控制扩展标签解析|

## 后端技术选型（CORS 代理服务）

|层面|推荐方案|选型理由|
|---|---|---|
|运行时|Node\.js \+ Express|与前端同语言，部署简单|
|代理实现|http\-proxy\-middleware|成熟的反向代理中间件|
|部署|Vercel / Cloudflare Workers / Docker|Serverless 方案免运维，适合低流量代理|

## 关键模块设计

### m3u 解析器

```typescript
interface M3uChannel {
  name: string;           // 频道名称
  url: string;            // 流地址
  logo?: string;          // 频道 Logo
  group?: string;         // 分组名称
  tvgId?: string;         // EPG ID
  duration: number;       // 时长（-1 表示直播）
  raw?: Record<string, string>; // 原始扩展属性
}

interface M3uPlaylist {
  channels: M3uChannel[];
  groups: string[];       // 去重后的分组列表
  rawContent: string;     // 原始内容（用于调试）
}

function parseM3u(content: string): M3uPlaylist;
```

### 播放器适配层

基于 video\.js 封装统一播放器组件：video\.js 内置 http\-streaming（VHS）模块，自动兼容 Safari 原生 HLS 与其他浏览器的 MSE 播放，对外暴露统一控制接口：

```typescript
import videojs from "video.js";

// 封装 video.js，统一对外控制接口
function createVideoJsPlayer(videoEl: HTMLVideoElement, options) {
  const player = videojs(videoEl, {
    controls: true,
    fluid: true,          // 自适应容器宽度
    html5: { vhs: { overrideNative: !videojs.browser.IS_SAFARI } },
    // Safari 使用原生 HLS，其余浏览器走 MSE
  });

  return {
    load(url: string) { player.src({ src: url, type: "application/x-mpegURL" }); },
    play() { return player.play(); },
    pause() { player.pause(); },
    seek(t: number) { player.currentTime(t); },
    setVolume(v: number) { player.volume(v); },
    on(event: string, cb: Function) { player.on(event, cb); },
    destroy() { player.dispose(); },
  };
}
```

# 页面与交互设计

## 页面结构

|页面|路由|核心内容|
|---|---|---|
|首页|/|URL 输入框、文件上传按钮、已保存播放列表、最近播放、收藏|
|播放页|/player|左侧分组\+频道列表，右侧播放器，顶部播放列表名称与搜索|
|收藏页|/favorites|收藏的频道列表，支持搜索和移除|
|设置页|/settings|播放列表管理、缓存清理、关于、免责声明|

## 播放页布局

- **桌面端（≥1024px）**：左侧 280px 频道列表面板（可折叠），右侧自适应播放器区域

- **平板端（768\-1023px）**：频道列表改为抽屉式，点击按钮从左侧滑出

- **移动端（\<768px）**：播放器置顶，频道列表在下方，分组改为顶部 Tab 横向滚动

# 接口设计（后端代理）

## CORS 代理接口

**用途**：解决播放列表加载时的跨域问题（视频流本身的跨域由 hls\.js 处理，无需代理）

|**接口**|GET /api/proxy?url=\{encoded\_m3u\_url\}|
|---|---|
|**请求参数**|url：目标 m3u/m3u8 地址，URL 编码|
|**响应**|直接透传目标响应内容，设置 CORS 头允许前端访问|
|**限制**|超时 10 秒，响应体 ≤ 10MB，仅允许 http/https 协议|
|**错误**|超时返回 504，目标不可达返回 502，参数非法返回 400|

**安全注意**：代理接口需做 SSRF 防护，禁止请求内网地址（10\.x、172\.16\-31\.x、192\.168\.x、127\.x、localhost），仅允许标准 80/443 端口。

# 异常与边界场景

|场景|系统行为|用户提示|
|---|---|---|
|播放列表为空（0 频道）|不进入播放页|"该播放列表中没有可播放的频道"|
|部分频道流地址无效|解析时标记为无效，列表中置灰|hover 显示"地址格式无效"|
|直播流中断|自动重试 1 次，失败后展示错误|"直播信号中断，请稍后重试"|
|点播流 404|立即停止，展示错误|"视频源不存在或已被移除"|
|网络断开|暂停播放，监听 online 事件|"网络已断开，恢复后将自动重试"|
|localStorage 已满|停止写入，清理最旧记录|"本地存储空间不足，已自动清理旧记录"|
|超大播放列表（\>5000 频道）|启用虚拟滚动，分批渲染|加载时显示进度提示|
|浏览器不支持 MSE|检测后降级提示|"您的浏览器不支持 HLS 播放，请使用最新版 Chrome/Safari"|

# 验收标准

## 功能验收

1. 能通过 URL 成功加载并解析标准 m3u/m3u8 播放列表，频道数量与源文件一致

2. 能通过本地文件上传加载播放列表，解析结果正确

3. 播放列表按 group\-title 正确分组，分组导航和搜索功能正常

4. 点击频道后能正常播放 HLS 视频流，音画同步

5. 纯音频流自动切换为音频模式，播放正常

6. 播放控制（播放/暂停、音量、全屏、进度条）全部可用

7. 倍速播放对点播内容生效，对直播内容禁用

8. 最近播放和收藏功能正常，数据刷新页面后不丢失

9. 播放列表管理（保存、切换、删除）功能正常

10. 所有异常场景均有明确的用户提示，不出现白屏或未捕获错误

## 兼容性验收

1. 在 Chrome、Edge、Firefox、Safari 最新版中核心功能全部可用

2. 在 iOS Safari 和 Android Chrome 中播放和基础交互正常

3. 在 375px、768px、1024px、1920px 宽度下布局无错乱

## 性能验收

1. 首屏 LCP ≤ 2 秒（4G 网络，Lighthouse 测试）

2. 1000 频道的播放列表解析时间 ≤ 1 秒

3. 页面滚动频道列表时帧率 ≥ 50fps

4. 播放 1080p 视频 30 分钟无内存泄漏（内存增长 ≤ 50MB）

# 依赖与开放问题

## 外部依赖

- video\.js（含内置 http\-streaming 模块，Apache\-2\.0 协议）

- Vue 3 及生态（MIT 协议）

- 用户提供的 m3u/m3u8 播放列表源（第三方，可用性不可控）

## 关键假设

- 用户的播放列表源支持 HTTPS 或可通过代理访问

- 播放内容为标准 HLS 格式，不含 DRM 加密

- 网站部署在支持 HTTPS 的静态托管或 Node\.js 环境上

## 决策记录与开放问题

|编号|问题|影响范围|负责人|
|---|---|---|---|
|Q1|是否需要后端 CORS 代理？|架构、部署|✅ 已确认：需要|
|Q2|是否需要 EPG（电子节目单）功能？|功能范围|✅ 已确认：不需要|
|Q3|播放器选择 video\.js 还是自研轻量播放器？|开发量、体积|✅ 已确认：video\.js|
|Q4|是否需要多语言支持（i18n）？|功能范围|待确认|
|Q5|是否需要 PWA 支持（离线访问、添加到主屏幕）？|功能范围|待确认|

# 里程碑建议

|阶段|周期（参考）|交付内容|
|---|---|---|
|M1 基础版|第 1\-2 周|项目搭建、m3u 解析器、基础播放器（video\.js 接入）、URL 加载、频道列表展示、后端 CORS 代理服务搭建|
|M2 核心版|第 3\-4 周|分组导航、搜索、播放控制完善、本地文件上传、错误处理、响应式布局|
|M3 增强版|第 5\-6 周|最近播放、收藏、播放列表管理、倍速、画中画、性能优化、兼容性测试|
|M4 发布|第 7 周|部署上线、文档编写、验收测试、Bug 修复|

---

**文档维护说明**：本文档为需求基线，后续变更需通过评审后更新版本号。开放问题确认后应及时更新对应章节。

> （注：部分内容可能由 AI 生成）
